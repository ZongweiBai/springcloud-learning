spring:
  profiles:
    active: dev
  application:
    name: cloud-user-server
  #  zipkin:
  #    base-url: http://localhost:9411
  cloud:
    consul:
      host: 192.168.3.10
      port: 8500
      discovery:
        instance-id: ${spring.application.name}:${spring.cloud.client.ip-address}
        service-name: user-service-provider
    nacos:
      config:
        server-addr: 192.168.3.10:8848
        file-extension: yaml