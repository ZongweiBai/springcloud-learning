spring:
  application:
    name: cloud-service-provider
#  zipkin:
#    base-url: http://localhost:9411
  cloud:
    consul:
        host: localhost
        port: 8500
        discovery:
          instance-id: ${spring.application.name}
          service-name: cloud-service-provider
    config:
      discovery:
        enabled: true
        service-id: cloud-config-server
      fail-fast: true
      username: root
      password: booszy
      profile: csdn